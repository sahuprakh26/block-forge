# Netlify live site deploys via GitHub → Netlify (project: blackforgee26).
# Do NOT use netlify deploy --allow-anonymous here — it creates unclaimed Drop URLs (401).

name: Netlify Drop (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use https://blackforgee26.netlify.app — connected in Netlify dashboard."
