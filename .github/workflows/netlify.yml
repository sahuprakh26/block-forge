# DEPRECATED — use GitHub Pages (.github/workflows/pages.yml) instead.
# Netlify account suspended; do not deploy here.

name: Netlify Drop (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use https://blackforgee26.netlify.app — connected in Netlify dashboard."
