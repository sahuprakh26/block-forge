name = "block-forge-api"
main = "worker/leaderboard-api.js"
compatibility_date = "2024-01-01"

[vars]
NODE_ENV = "production"

# Secrets (GITHUB_GIST_ID, GITHUB_TOKEN) set via wrangler secret or GitHub Actions
